'use strict';

describe('myApp.permission module', function() {
  beforeEach(module('myApp.permission'));

  describe('permission controller', function() {
    it('should ....', inject(function($controller) {
      //spec body
      var permissionCtrl = $controller('permissionCtrl');
      expect(permissionCtrl).toBeDefined();
    }));
  });
});
